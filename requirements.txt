# ============================================================
# PizzaRia — Dependências do Backend (Flask + Pydantic + JWT)
# ============================================================
# Python >= 3.10 (testado em 3.14)
#
# Uso:
#   cd backend
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r ../requirements.txt
# ============================================================

# Flask 3.x — microframework web (rotas, request, jsonify, Blueprint)
Flask==3.1.3

# Flask-CORS — libera requisições cross-origin pro frontend (Vite :5173)
Flask-CORS==6.0.2

# Pydantic v2 — validação de schemas (BaseModel, EmailStr, field_validator)
# Depende do pydantic-core (Rust); instala automaticamente
pydantic>=2.11,<3

# email-validator — validação de e-mail usada pelo EmailStr do Pydantic
email-validator==2.2.0

# PyJWT — geração e verificação de tokens JWT (import jwt no Python)
PyJWT==2.10.1